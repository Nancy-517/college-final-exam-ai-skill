# Contributing

欢迎改进工作流、模板、学科 profile、匿名示例和评估方法。

提交前请确认：

- 不包含教师 PPT、内部试卷、答案原文或未授权教材内容。
- 不包含学校、教师、学生的可识别隐私信息。
- 示例已改写、匿名化，且用于说明方法而非传播题库。
- 新增规则说明其适用条件，不把单一课程经验写成普遍结论。
- `SKILL.md` 保持精简；详细内容放入 `references/`。
- 运行 `quick_validate.py` 并检查 Markdown 链接。

建议的提交类型：

- `feat:` 新增工作流或学科 profile
- `fix:` 修复错误或歧义
- `docs:` 改进文档与示例
- `test:` 增加验证场景
